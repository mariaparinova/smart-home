import { HighlightActiveDevice } from './highlight-active-device';

describe('HighlightActiveDevice', () => {
  it('should create an instance', () => {
    const directive = new HighlightActiveDevice();
    expect(directive).toBeTruthy();
  });
});
